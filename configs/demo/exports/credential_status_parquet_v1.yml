schema_version: "1.0"
export_id: credential_status_parquet_v1
version: "1.0"
mart_id: credential_status_v1
format: parquet
fields: [assessment_person_id, credential_code, awarded_at, expires_at, current_status]
masked_fields: [assessment_person_id]
retention_days: 30
