# ADR 0008: Masked Profile Evidence

Status: Accepted

Profiles persist counts, rates, numeric summaries, schema types, and fingerprinted top values. Raw samples are not stored. Drift comparisons always expose metric deltas and thresholds so a status can be reviewed rather than treated as universal truth.
