# ADR 0004: Internal UUIDs and Source-Scoped Identities

Status: Accepted

Canonical records use UUIDs. Source identifiers remain separate ExternalIdentity records scoped by organization and source. Email is not treated as a permanent global identity. Organization-owned queries must be explicitly scoped.
