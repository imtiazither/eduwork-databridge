# ADR 0011: Explicit Probabilistic Estimation and Review Thresholds

Status: Accepted

Probabilistic parameters are estimated only when a named synthetic or approved labeled truth set is explicitly supplied. Model versions and estimation provenance are persisted. Human decisions never trigger automatic retraining. Thresholds are illustrative until deployment-specific calibration and approval.
